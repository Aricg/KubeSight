# KubeSight
chroma vectordb embeddings for kubernetes home lab

```
(venv)  ~/Github/KubeSight   main  ./embedhelm.py -q "how many options are in this helm chart?"
Number of requested results 4 is greater than number of elements in index 1, updating n_results = 1
 The helm chart contains 17 configuration options.
(venv)  ~/Github/KubeSight   main  ./embedhelm.py -q "what are the configuration options?"
Number of requested results 4 is greater than number of elements in index 1, updating n_results = 1
 The configuration options are EULA, TYPE, VERSION, DIFFICULTY, MAX_PLAYERS, MAX_WORLD_SIZE, MAX_BUILD_HEIGHT, MAX_TICK_TIME, SPAWN_PROTECTION, VIEW_DISTANCE, MODE, MOTD, LEVEL_TYPE, LEVEL, MODRINTH_ALLOWED_VERSION_TYPE, MEMORY, OVERRIDE_SERVER_PROPERTIES, ENABLE_RCON.

```
